// import Ember from 'ember';
import { moduleForComponent, test } from 'ember-qunit';
// import hbs from 'htmlbars-inline-precompile';

moduleForComponent('modal-dialog-ok-cancel', 'Integration | Component | modal dialog ok cancel', {
	integration: true
});

test('it renders', function(assert) {

	assert.ok(true);
	// Set any properties with this.set('myProperty', 'value');
	// Handle any actions with this.on('myAction', function(val) { ... });

	// this.render(hbs`{{modal-dialog-ok-cancel showDialog="true"}}`);
  //
	// Ember.run(() => {
	// 	this.set('showDialog', true);
	// });
  //
	// var $component = this.$('.modal-dialog-ok-cancel');
	//
	// assert.equal(this.$('.modal-btn-ok').text().trim(), 'OK');
	// assert.equal(this.$('.modal-btn-cancel').text().trim(), 'Cancel');

	// Template block usage:
	// this.render(hbs`
	// 		{{#modal-dialog-ok-cancel}}
	// 		template block text
	// 		{{/modal-dialog-ok-cancel}}
	// 		`);
  //
	// this.set('showDialog', true);
	// assert.ok(this.$().text().indexOf('template block text') !== -1);
});
